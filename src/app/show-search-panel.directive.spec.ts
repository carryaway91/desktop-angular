import { ShowSearchPanelDirective } from './show-search-panel.directive';

describe('ShowSearchPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowSearchPanelDirective();
    expect(directive).toBeTruthy();
  });
});
