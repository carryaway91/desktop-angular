import { ShowSublistDirective } from './show-sublist.directive';

describe('ShowSublistDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowSublistDirective();
    expect(directive).toBeTruthy();
  });
});
